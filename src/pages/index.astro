---
import PageLayout from '@layouts/PageLayout.astro';
import Mision from '@sections/home/Mision.astro';
import BentoGallery from '@sections/home/BentoGallery.astro';
import FeaturedArtists from '@sections/home/FeaturedArtists.astro';
import LatestNewsReleases from '@sections/home/LatestNews&Releases.astro';

import UpcomingEvents from '@sections/home/UpcomingEvents.astro';
import Team from '@sections/home/Team.astro';

import { getInfoPage } from '@libs/apiPages.ts';

const pageSlugIndex = 'inicio';

const { 
	pageSlug, 
	pageTitle, 
	heroTitle, 
	heroDescription, 
	heroButton,
	featureTitle, 
	featureDescription,
	featureButton, 
	teamTitle, 
	teamDescription,
	teamButton,
	artistsTitle,
	artistsSubtitle,
	artistButton,
	eventsTitle,
	eventsSubtitle,
	eventButton,
	newsTitle,
	newsSubtitle,
	newsButton,
} = await getInfoPage(pageSlugIndex);


---
<PageLayout
  slug={pageSlug}
  pageTitle={pageTitle}
  heroTitle={heroTitle}
  heroButton={heroButton}
>
<Mision 
	featureTitle={featureTitle}
	featureDescription={featureDescription}
	featureButton={featureButton}
/>
<FeaturedArtists 
	pageSlug={pageSlugIndex}
	artistsTitle={artistsTitle}
	artistButton={artistButton}
/>
<UpcomingEvents 
	pageSlug={pageSlugIndex}
	eventsTitle={eventsTitle}
	eventsSubtitle={eventsSubtitle}
	eventButton={eventButton}
/>
<LatestNewsReleases
	pageSlug={pageSlugIndex}
	newsTitle={newsTitle}
	newsSubtitle={newsSubtitle}
	newsButton={newsButton}
/>
<BentoGallery />
<Team 
	pageSlug={pageSlugIndex}
	teamTitle={teamTitle}
	teamDescription={teamDescription}
	teamButton={teamButton}
/>
	
</PageLayout>

